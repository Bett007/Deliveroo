VITE_API_BASE_URL=http://127.0.0.1:5001/api
# For production deployments, set this to your public backend URL instead of localhost.
# Use a public Mapbox token only (starts with `pk.`), never a secret token (`sk.`).
VITE_MAPBOX_TOKEN=
# Backward-compatible fallback:
VITE_MAPBOX_ACCESS_TOKEN=
